#ifndef SCENE_MAIN_MENU
#define SCENE_MAIN_MENU
#include "../Scene.cpp"

class MainMenuScene : public Scene {
private:
    /* data */
public:
    void init() override {
        // Inizializza il menu principale
    }

    void update() override {
        // Aggiornamento del menu principale (es. animazioni)
    }

    void input() override {
        // Gestione dell'input del menu principale
    }

    void render() override {
        // Disegna gli elementi del menu principale
    }

    void cleanup() override {
        // Pulizia delle risorse del menu principale
    }
};
#endif